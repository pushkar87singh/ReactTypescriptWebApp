import React from "react";
import Typography, { TypographyProps } from "@material-ui/core/Typography";

const Subtitle1 = (props: TypographyProps) => (
  <Typography variant="subtitle1" gutterBottom {...props} />
);

export default Subtitle1;
