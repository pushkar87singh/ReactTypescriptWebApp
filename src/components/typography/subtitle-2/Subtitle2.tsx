import React from "react";
import Typography, { TypographyProps } from "@material-ui/core/Typography";

const Subtitle2 = (props: TypographyProps) => (
  <Typography variant="subtitle2" gutterBottom {...props} />
);

export default Subtitle2;
